# 5.2 智能查询与辅助诊断模块

智能查询与辅助诊断模块解决日常诊疗模块沉淀的海量检查数据"查得到"和"看得懂"两个核心问题。三个子功能分别从不同层面提升数据的可用价值：多条件组合查询让用户以任意维度灵活筛选目标记录，指标趋势分析将分散的检验值转化为直观的时序曲线，多指标联合异常检测则从单指标超限判断升级为多维度综合评分，挖掘人工审核难以发现的关联异常模式。下面按这三个子功能依次展开。

## 5.2.1 多条件组合查询

多条件组合查询的核心不在于算法复杂，而在于 SQL 构建策略的灵活性。系统为四类检查（CT、核磁、病理、肠镜）和检验结果分别提供独立的查询面板，每类面板支持患者编号、姓名、检查编号、检查部位、检查医生、所属科室、检查时间范围等多个维度自由组合。本节以 CT 检查为例说明设计实现。

涉及的核心类包括：CtExaminationController（查询入口，暴露 /ctExamination/listPage 分页接口）、ICtExaminationService / CtExaminationServiceImpl（查询逻辑封装）、CtExaminationMapper（底层 MyBatis-Plus BaseMapper）以及 SearchTemplateController / SearchTemplateServiceImpl / SearchTemplateMapper / SearchTemplate（查询模板的保存、加载与共享功能）。

### 流程图

查询流程分为条件构造和模板管理两路。条件构造一路用 QueryWrapper 动态拼接仅含非空条件的 WHERE 子句，模板管理一路将当前筛选条件序列化为 JSON 存入 search_template 表以便复用。

```mermaid
flowchart TD
    A["医生打开查询面板"] --> B["填写筛选条件"]
    B --> C{"选择了哪些条件?"}
    C --> D["逐条检查每个条件字段是否非空"]
    D --> E{"条件字段是否非空?"}
    E -->|是| F["向 QueryWrapper 追加对应条件"]
    E -->|否| G["跳过该字段"]
    F --> H{"还有未检查的条件?"}
    G --> H
    H -->|是| D
    H -->|否| I["追加默认过滤条件: isInvalid=false"]
    I --> J["执行分页查询"]
    J --> K["返回分页结果到前端"]
    K --> L{"医生是否需要保存为模板?"}
    L -->|是| M["将当前条件序列化为JSON"]
    M --> N["写入 search_template 表"]
    N --> O["标记模板类型与共享状态"]
    L -->|否| P["完成查询"]
    O --> P
```

### 时序图

```mermaid
sequenceDiagram
    actor 医生 as 医生
    participant Controller as CtExaminationController
    participant Service as CtExaminationServiceImpl
    participant Mapper as CtExaminationMapper
    participant DB as ct_examination表
    participant TemplateService as SearchTemplateService

    医生->>Controller: POST /ctExamination/listPage
    Controller->>Controller: 提取查询条件Map
    Controller->>Service: page(QueryPageParam)
    Service->>Service: 创建 QueryWrapper 对象
    loop 遍历条件Map中每个键值对
        alt 值非空
            Service->>Service: wrapper.eq/like/between 追加条件
        else 值为空
            Service->>Service: 跳过此条件
        end
    end
    Service->>Service: wrapper.eq("is_invalid", false)
    Service->>Mapper: selectPage(page, wrapper)
    Mapper->>DB: SELECT ... WHERE ... LIMIT offset,size
    DB-->>Mapper: 返回当前页数据
    Mapper-->>Service: Page对象（含 total 和 records）
    Service-->>Controller: 分页结果
    Controller-->>医生: 返回列表与分页信息

    opt 保存为模板
        医生->>TemplateService: POST /searchTemplate/save
        TemplateService->>TemplateService: 将查询条件序列化为JSON
        TemplateService->>DB: INSERT INTO search_template
        DB-->>TemplateService: 保存成功
        TemplateService-->>医生: 返回模板ID
    end
```

## 5.2.2 检验指标趋势分析

趋势分析将某位患者某项检验指标的全部历史结果以折线图形式呈现，帮助医生快速判断指标的长期变化方向——是持续升高、逐步下降还是趋于稳定。该功能尤其适用于糖尿病血糖监测、肾功能肌酐随访等慢性病管理场景。

涉及的核心类包括：LabResultController（暴露 /labResult/getTrend 接口，接收 patientId 和 itemId 两个参数）、LabResultServiceImpl（实现 getTrendData 方法，查询历史数据并按时间排序）、LabResultMapper（执行按患者和项目的联合查询）、LabItemDictMapper（获取当前项目的最新参考范围字符串）以及 TrendVO（数据传输对象，封装时间、结果值、单位和参考范围四个字段）。

### 流程图

```mermaid
flowchart TD
    A["医生在患者详情页选择检验项目"] --> B["前端发送 GET /labResult/getTrend"]
    B --> C["LabResultController 提取 patientId 和 itemId"]
    C --> D["LabResultServiceImpl 调用 LabResultMapper"]
    D --> E["按患者编号与项目编码查询全部历史结果"]
    E --> F["结果按报告时间升序排列"]
    F --> G["逐条封装为 TrendVO 对象"]
    G --> H["调用 LabItemDictMapper 获取该项目参考范围"]
    H --> I["将参考范围字段填入每个 TrendVO"]
    I --> J["返回 TrendVO 列表至前端"]
    J --> K["前端用 ECharts 渲染折线图"]
    K --> L["横轴为报告日期，纵轴为检验值"]
    L --> M["用虚线标注参考范围上下限"]
    M --> N["异常值以红色标记突出显示"]
```

### 时序图

```mermaid
sequenceDiagram
    actor 医生 as 医生
    participant Controller as LabResultController
    participant Service as LabResultServiceImpl
    participant LabMapper as LabResultMapper
    participant DictMapper as LabItemDictMapper
    participant DB as lab_result表

    医生->>Controller: GET /labResult/getTrend?patientId=xxx&itemId=16981
    Controller->>Service: getTrend(patientId, itemId)
    Service->>LabMapper: selectList(按patientId和itemId查询)
    LabMapper->>DB: SELECT * FROM lab_result WHERE patient_id=? AND item_id=? ORDER BY report_time ASC
    DB-->>LabMapper: 返回该患者该项指标全部历史数据
    LabMapper-->>Service: List~LabResult~ 按时间升序
    Service->>DictMapper: selectById(itemId)
    DictMapper->>DB: SELECT normal_range FROM lab_item_dict WHERE item_id=?
    DB-->>DictMapper: 返回参考范围字符串
    DictMapper-->>Service: 参考范围
    loop 遍历每条检验记录
        Service->>Service: 将 reportTime, resultValue, resultUnit, normalRange 封装为 TrendVO
    end
    Service-->>Controller: List~TrendVO~
    Controller-->>医生: 返回趋势数据
    医生->>医生: ECharts 渲染折线图，标注参考范围上下限
```

## 5.2.3 多指标联合异常检测

多指标联合异常检测是智能查询模块中算法最复杂的子功能。当医生怀疑某位患者可能存在代谢或血液系统问题时，可选择一组检验指标（如肝功能组合或血脂组合）发起联合检测。系统通过 Z-Score 标准化消除各指标量纲差异，再调用预训练的孤立森林模型从多指标组合维度计算异常分数，最终输出"正常 / 可疑 / 异常"三级判定结论及异常贡献最高的指标清单。

涉及的核心组件包括：LabResultController（暴露 /labResult/jointDetect 接口）、LabResultServiceImpl（协调检测流程）、IsolationForestServiceImpl（实现 jointDetect 核心算法，封装 Z-Score 标准化、孤立森林评分与综合判定三类逻辑）、IsolationForestMapper / IsolationForestModelMapper（持久化孤立森林规则与训练模型）、IsolationForest（纯 Java 实现的孤立森林算法类，封装 buildForest 训练方法和 predict 预测方法）、IsolationForestRule / IsolationForestModel（规则与模型的实体类）、LabItemStatistics（各检验项目历史均值和标准差的统计记录）以及 IStatisticsComputeService（负责 Z-Score 标准化所需的均值和标准差数据的计算与缓存）。

### 流程图

```mermaid
flowchart TD
    A["医生选择患者和多项检验指标"] --> B["前端发送 POST /labResult/jointDetect"]
    B --> C["LabResultServiceImpl 查询患者最近一次的多指标检验结果"]
    C --> D{"所有指定项目都有检验值?"}
    D -->|否| E["返回提示：部分项目数据缺失"]
    D -->|是| F["进入 Z-Score 标准化阶段"]
    F --> G["逐项查询 lab_item_statistics 表获取均值和标准差"]
    G --> H["对每项指标计算 Z = (实际值 - 均值) / 标准差"]
    H --> I["得到标准化特征向量"]
    I --> J{"是否存在匹配的 IsolationForestRule?"}
    J -->|是| K["进入孤立森林评分阶段"]
    J -->|否| L["仅基于 Z-Score 给出参考分析"]
    K --> M["加载或训练 IsolationForest 模型"]
    M --> N["将特征向量遍历全部 100 棵孤立树"]
    N --> O["计算样本在所有树中的平均路径长度"]
    O --> P["通过归一化公式转换为异常分数"]
    P --> Q["进入综合判定阶段"]
    L --> Q
    Q --> R["Z-Score 偏离值与孤立森林分数加权融合"]
    R --> S{"综合分数是否超过阈值?"}
    S -->|超过| T["判定为：异常"]
    S -->|略低于阈值| U["判定为：可疑"]
    S -->|远低于阈值| V["判定为：正常"]
    T --> W["输出异常结论与偏离指标清单"]
    U --> W
    V --> W
```

### 时序图

```mermaid
sequenceDiagram
    actor 医生 as 医生
    participant Controller as LabResultController
    participant LabService as LabResultServiceImpl
    participant IFService as IsolationForestServiceImpl
    participant Forest as IsolationForest
    participant StatService as StatisticsComputeService
    participant RuleMapper as IsolationForestMapper
    participant StatTable as lab_item_statistics表
    participant ModelTable as isolation_forest_model表

    医生->>Controller: POST /labResult/jointDetect
    Controller->>LabService: jointIsolationForestDetect(patientId, itemIds)
    LabService->>LabService: 查询该患者最近的多指标检验结果
    LabService->>IFService: jointDetect(results, rule)
    
    Note over IFService: 第一阶段 Z-Score 标准化
    loop 遍历每个检验指标
        IFService->>StatService: getStatistics(itemId)
        StatService->>StatTable: SELECT mean, std FROM lab_item_statistics
        StatTable-->>StatService: 返回均值与标准差
        StatService-->>IFService: 统计参数
        IFService->>IFService: 计算 Z = (X - μ) / σ
    end
    
    Note over IFService: 第二阶段 孤立森林评分
    IFService->>RuleMapper: 查询 ruleId 对应规则
    RuleMapper-->>IFService: IsolationForestRule
    IFService->>Forest: getOrBuildModel(rule)
    Forest->>ModelTable: 反序列化加载模型JSON
    ModelTable-->>Forest: 模型参数
    Forest->>Forest: 遍历100棵iTree计算路径长度
    Forest-->>IFService: 异常分数
    
    Note over IFService: 第三阶段 综合判定
    IFService->>IFService: 加权融合 Z-Score 与孤立森林分数
    IFService->>IFService: 与 threshold 比较得出 正常/可疑/异常
    IFService-->>LabService: IsolationResult(异常等级 + 指标清单)
    LabService-->>Controller: IsolationForestResultVO
    Controller-->>医生: 返回联合检测结论
```

## 智能查询与辅助诊断模块总类图

下面这张类图汇总了智能查询与辅助诊断模块全部核心类及其依赖关系。图中上半区为查询分析线（从各类检查 Controller 到 Mapper），下半区为孤立森林算法线（从 LabResultController 逐层穿透到 IsolationForest 核心算法类），左侧为查询模板辅助线，底部分别为统计计算（Z-Score 数据来源）和 Redis 缓存加速（热点数据缓存）两条支撑线。

```mermaid
classDiagram
    class CtExaminationController {
        +listPage(QueryPageParam) Result
        +export(QueryPageParam,HttpServletResponse) void
    }
    class CtExaminationServiceImpl {
        +page(QueryPageParam) Page
    }
    class CtExaminationMapper {
    }

    class MriExaminationController {
        +listPage(QueryPageParam) Result
    }
    class MriExaminationServiceImpl {
    }
    class MriExaminationMapper {
    }

    class PathologyExaminationController {
        +listPage(QueryPageParam) Result
    }
    class PathologyExaminationServiceImpl {
    }
    class PathologyExaminationMapper {
    }

    class EnteroscopyExaminationController {
        +listPage(QueryPageParam) Result
    }
    class EnteroscopyExaminationServiceImpl {
    }
    class EnteroscopyExaminationMapper {
    }

    class LabResultController {
        +listPage(QueryPageParam) Result
        +getTrend(String,int) Result
        +jointDetect(JointDetectRequest) Result
    }
    class ILabResultService {
        <<interface>>
        +getTrend(String,int) Result
        +jointIsolationForestDetect(String,List) IsolationForestResultVO
    }
    class LabResultServiceImpl {
        -LabResultMapper labResultMapper
        -LabItemDictMapper dictMapper
        -IsolationForestService ifService
        +getTrend(String,int) Result
        +jointIsolationForestDetect(String,List) IsolationForestResultVO
    }
    class LabResultMapper {
    }
    class LabItemDictMapper {
    }

    class SearchTemplateController {
        +save(SearchTemplate) Result
        +listByUser(Integer) Result
        +delete(Long) Result
    }
    class ISearchTemplateService {
        <<interface>>
    }
    class SearchTemplateServiceImpl {
    }
    class SearchTemplateMapper {
    }
    class SearchTemplate {
        -Long templateId
        -String templateName
        -String templateType
        -String queryConditions
        -Integer userId
        -Boolean isShared
    }

    class IIsolationForestService {
        <<interface>>
        +jointDetect(List,IsolationForestRule) IsolationResult
        +trainModel(Long) void
        +rebuildAllModels() void
        +getAllEnabledRules() List
    }
    class IsolationForestServiceImpl {
        -IsolationForestMapper ruleMapper
        -IsolationForestModelMapper modelMapper
        -LabResultMapper labResultMapper
        -LabItemDictMapper dictMapper
        -StatisticsComputeService statService
        -Map~Long,IsolationForest~ modelCache
        +jointDetect(List,IsolationForestRule) IsolationResult
        +buildModel(IsolationForestRule) void
        -buildFeatureVector(List,List) double[]
        -zscoreDetect(List,List) Map
        -combinedScoreMethod(Map,double) double
        -determineAnomalyLevel(double,Map) String
    }
    class IsolationForest {
        -List~IsolationTree~ trees
        -int numTrees
        -int maxDepth
        -int sampleSize
        +buildForest(List~double[]~,int,int) void
        +predict(double[]) double
    }
    class IsolationTree {
        -TreeNode root
        +build(double[][],int,int) void
    }
    class TreeNode {
        -int featureIndex
        -double splitValue
        -TreeNode left
        -TreeNode right
        -int size
        -boolean isLeaf
    }

    class IsolationForestMapper {
        +selectById(Long) IsolationForestRule
        +selectList(Wrapper) List
    }
    class IsolationForestModelMapper {
        +selectOne(Wrapper) IsolationForestModel
        +insert(IsolationForestModel) int
        +updateById(IsolationForestModel) int
    }
    class IsolationForestRule {
        -Long ruleId
        -String ruleName
        -String itemIds
        -BigDecimal contamination
        -BigDecimal thresholdScore
        -Boolean enabled
        -String severity
        -String description
    }
    class IsolationForestModel {
        -Long modelId
        -Long ruleId
        -String itemIds
        -String modelParams
        -LocalDateTime trainedAt
        -Integer sampleCount
        -BigDecimal meanScore
        -BigDecimal stdScore
    }

    class IStatisticsComputeService {
        <<interface>>
        +getStatistics(int,String) LabItemStatistics
        +updateStatistics(int,String) void
    }
    class StatisticsComputeServiceImpl {
        -LabResultMapper labResultMapper
        -RedisTemplate redisTemplate
        +getStatistics(int,String) LabItemStatistics
    }
    class LabItemStatistics {
        -Long statId
        -Integer itemId
        -String deptCode
        -Integer sampleCount
        -BigDecimal meanValue
        -BigDecimal stdDeviation
        -BigDecimal minValue
        -BigDecimal maxValue
    }

    class TrendVO {
        -LocalDateTime reportTime
        -BigDecimal resultValue
        -String resultUnit
        -String normalRange
    }
    class IsolationForestResultVO {
        -boolean triggered
        -String anomalyLevel
        -double isolationScore
        -double combinedScore
        -double threshold
        -String alertMessage
        -Map~Integer,Double~ zscoreAnomalies
    }
    class JointDetectRequest {
        -String patientId
        -List~Integer~ itemIds
    }

    CtExaminationController --> CtExaminationServiceImpl
    CtExaminationServiceImpl --> CtExaminationMapper
    MriExaminationController --> MriExaminationServiceImpl
    MriExaminationServiceImpl --> MriExaminationMapper
    PathologyExaminationController --> PathologyExaminationServiceImpl
    PathologyExaminationServiceImpl --> PathologyExaminationMapper
    EnteroscopyExaminationController --> EnteroscopyExaminationServiceImpl
    EnteroscopyExaminationServiceImpl --> EnteroscopyExaminationMapper

    LabResultController --> ILabResultService
    ILabResultService <|.. LabResultServiceImpl
    LabResultServiceImpl --> LabResultMapper
    LabResultServiceImpl --> LabItemDictMapper
    LabResultServiceImpl --> IIsolationForestService
    LabResultServiceImpl ..> TrendVO
    LabResultController ..> JointDetectRequest

    SearchTemplateController --> ISearchTemplateService
    ISearchTemplateService <|.. SearchTemplateServiceImpl
    SearchTemplateServiceImpl --> SearchTemplateMapper
    SearchTemplateMapper ..> SearchTemplate

    IIsolationForestService <|.. IsolationForestServiceImpl
    IsolationForestServiceImpl --> IsolationForestMapper
    IsolationForestServiceImpl --> IsolationForestModelMapper
    IsolationForestServiceImpl --> LabResultMapper
    IsolationForestServiceImpl --> LabItemDictMapper
    IsolationForestServiceImpl --> IStatisticsComputeService
    IsolationForestServiceImpl --> IsolationForest
    IsolationForestServiceImpl ..> IsolationForestRule
    IsolationForestServiceImpl ..> IsolationForestModel
    IsolationForestServiceImpl ..> LabItemStatistics
    LabResultServiceImpl ..> IsolationForestResultVO

    IsolationForest *-- IsolationTree
    IsolationTree *-- TreeNode
    IsolationForestMapper ..> IsolationForestRule
    IsolationForestModelMapper ..> IsolationForestModel

    IStatisticsComputeService <|.. StatisticsComputeServiceImpl
    StatisticsComputeServiceImpl --> LabResultMapper
    StatisticsComputeServiceImpl ..> LabItemStatistics
```
