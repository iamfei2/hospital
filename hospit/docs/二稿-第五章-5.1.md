# 5.1 日常诊疗模块

日常诊疗模块是整个系统的数据基础层，后续智能查询、统计分析、预警监控等所有业务均依赖本模块产生的检查与检验数据。实现上采用 Spring MVC 标准三层架构：Controller 层负责接收 HTTP 请求、校验参数并封装响应，Service 层承载全部业务规则与事务控制，Mapper 层借助 MyBatis-Plus 的 BaseMapper 完成持久化操作。

本模块共涉及七个 Controller 类、七个 Service 接口及实现类、七个 Mapper 接口、十二个实体类以及一个 AOP 切面类。其中四类检查（CT、核磁、病理、肠镜）在代码层面结构完全对称——每种检查类型独立配一组 Controller-Service-Mapper-Entity 四件套，Service 实现类统一继承 MyBatis-Plus 的 ServiceImpl，无需手写基础增删改查代码。下面按患者信息管理、检查记录管理、XML 解析导入、检验结果管理和医嘱管理五个子功能依次展开。

## 5.1.1 患者信息管理

患者信息管理是系统所有数据的入口，负责患者档案的创建、修改、检索与全景汇总查询。本节设计了 PatientController、PatientServiceImpl、PatientMapper 以及 Patient 实体四个核心组件，同时引入 PatientOverviewVO 作为跨表聚合查询的数据传输对象。

### 流程图

患者信息新增从接收表单到完成归档共经历五个处理节点：参数校验、生成编号、敏感字段加密、数据库写入和操作日志记录。其中参数校验由 Controller 层借助 Hibernate Validator 注解完成，敏感信息加密调用 AESUtil工具类在 Service 层执行，操作日志由标注在 Controller 方法上的 @OperateLog 注解触发 AOP 切面异步写入。

```mermaid
flowchart TD
    A["前端提交患者信息表单"] --> B["PatientController 接收请求"]
    B --> C{"参数合法性校验"}
    C -->|校验失败| D["返回错误提示"]
    C -->|校验通过| E["PatientServiceImpl 生成唯一患者编号"]
    E --> F["对身份证号与手机号进行 AES 加密"]
    F --> G["调用 PatientMapper.insert 写入数据库"]
    G --> H["AOP 切面获取操作上下文"]
    H --> I["异步记录新增操作日志"]
    I --> J["返回登记成功"]
```

### 时序图

患者新增过程中，Controller 调用 Service、Service 调用 Mapper 的三层调用链路，以及 AOP 切面在目标方法执行后的拦截与日志异步写入，完整时序如图 5-2 所示。

```mermaid
sequenceDiagram
    actor 前端 as 前端
    participant Controller as PatientController
    participant Service as PatientServiceImpl
    participant Mapper as PatientMapper
    participant DB as patient表
    participant AOP as OperateLogAspect

    前端->>Controller: POST /patient/save
    Controller->>Controller: 校验必填字段与身份证号格式
    alt 校验不通过
        Controller-->>前端: 返回参数错误
    else 校验通过
        Controller->>Service: addPatient(Patient)
        Service->>Service: 生成统一患者编号
        Service->>Service: AES加密 phone 和 idCard
        Service->>Mapper: insert(patient)
        Mapper->>DB: INSERT INTO patient
        DB-->>Mapper: 写入成功
        Mapper-->>Service: 返回实体
        Service-->>Controller: 返回患者信息
        Controller->>AOP: @OperateLog 触发环绕通知
        AOP->>AOP: 获取IP与操作人信息
        AOP->>AOP: 异步写入 operation_log 表
        Controller-->>前端: 返回登记成功
    end
```

患者全景查询的时序如图所示：PatientController 的 aggregatePatientOverview 方法并行调用六类数据的 Mapper，将同一患者分散在检查、检验、医嘱各表中的历史记录一次性聚合后返回。

```mermaid
sequenceDiagram
    actor 医生 as 医生
    participant Controller as PatientController
    participant Service as PatientServiceImpl
    participant CT_Mapper as CtExaminationMapper
    participant MRI_Mapper as MriExaminationMapper
    participant PATH_Mapper as PathologyExaminationMapper
    participant ENTERO_Mapper as EnteroscopyExaminationMapper
    participant LAB_Mapper as LabResultMapper
    participant ORDER_Mapper as MedicalOrderMapper

    医生->>Controller: GET /patient/overview/{patientId}
    Controller->>Service: getPatientOverview(patientId)
    par 并行查询六张数据表
        Service->>CT_Mapper: selectList(按patientId查询)
        CT_Mapper-->>Service: CT检查列表
        Service->>MRI_Mapper: selectList(按patientId查询)
        MRI_Mapper-->>Service: MRI检查列表
        Service->>PATH_Mapper: selectList(按patientId查询)
        PATH_Mapper-->>Service: 病理检查列表
        Service->>ENTERO_Mapper: selectList(按patientId查询)
        ENTERO_Mapper-->>Service: 肠镜检查列表
        Service->>LAB_Mapper: selectList(按patientId查询)
        LAB_Mapper-->>Service: 检验结果列表
        Service->>ORDER_Mapper: selectList(按patientId查询)
        ORDER_Mapper-->>Service: 医嘱列表
    end
    Service->>Service: 合并六类结果，按时间倒序排列
    Service-->>Controller: PatientOverviewVO
    Controller-->>医生: 返回就诊全景数据
```

## 5.1.2 检查记录管理

检查记录管理覆盖 CT、核磁、病理、肠镜四类检查的全流程操作。四类检查各对应一组独立的 Controller-Service-Mapper-Entity 组件：CtExaminationController / ICtExaminationService / CtExaminationServiceImpl / CtExaminationMapper / CtExamination，其余三类类推。所有 Service 实现类均继承 MyBatis-Plus 的 ServiceImpl，内置通用 CRUD 方法，无需手写基础增删改查代码。

### 流程图

以 CT 检查为例，从录入到发布报告的完整路径如图所示。其中作废与恢复操作的判断分支体现了逻辑删除的核心设计：作废不是物理删除，而是将 isInvalid 字段翻转；所有查询默认过滤 isInvalid=1 的记录，恢复操作则将标识重置。

```mermaid
flowchart TD
    A["医生填写检查信息表单"] --> B["前端提交至对应Controller"]
    B --> C{"操作类型判断"}
    C -->|新增| D["Service层校验患者编号是否存在"]
    C -->|修改| E["Service层查询原记录快照"]
    C -->|作废| F["将 isInvalid 置为 1"]
    C -->|恢复| G["将 isInvalid 置为 0"]
    D --> H{"患者编号有效?"}
    H -->|否| I["返回错误提示"]
    H -->|是| J["生成检查编号并写入数据库"]
    E --> K["更新检查记录字段"]
    K --> L["AOP切面记录修改前后数据差异"]
    F --> M["AOP切面记录作废操作日志"]
    G --> M
    J --> N["AOP切面记录新增操作日志"]
    L --> O["返回操作成功"]
    M --> O
    N --> O
```

### 时序图

附件上传流程中，Controller 接收 MultipartFile 对象，Service 层生成带时间戳的唯一文件名以避免覆盖，将物理文件写入服务器本地目录后，再将文件访问路径回写到对应检查记录的 reportUrl 字段。

```mermaid
sequenceDiagram
    actor 医生 as 医生
    participant Controller as CtExaminationController
    participant Service as CtExaminationServiceImpl
    participant FileSys as 本地文件系统
    participant DB as ct_examination表

    医生->>Controller: POST /ctExamination/upload
    Controller->>Controller: 校验文件格式(PDF/图片)
    alt 格式不合法
        Controller-->>医生: 返回格式错误
    else 格式合法
        Controller->>Service: uploadFile(MultipartFile, ctId)
        Service->>Service: 生成文件名(时间戳_原始文件名)
        Service->>FileSys: 写入 uploads/ct/ 目录
        FileSys-->>Service: 写入成功，返回存储路径
        Service->>DB: UPDATE report_url
        DB-->>Service: 更新成功
        Service-->>Controller: 返回文件访问URL
        Controller-->>医生: 上传成功，返回预览链接
    end
```

## 5.1.3 XML 解析导入

XML 解析导入是检查记录创建的另一种途径：除手工表单填报外，系统支持接收 HIS、LIS 等外部系统导出的 XML 格式检查报告文件，通过自研 DOM 解析器自动提取字段并写入对应检查记录表。该功能并非批量历史数据迁移，而是一种单条记录录入方式，与批量 Excel 导入在业务定位上有本质区别。

XML 导入涉及的核心类包括：XmlImportHandler（导入入口控制器，负责接收文件并调度解析）、AbstractXmlParser（五种解析器的抽象基类，封装日期兼容解析和 XPath 路径查询的共用逻辑）、CtXmlParser / MriXmlParser / EnteroscopyXmlParser / PathologyXmlParser / LabXmlParser（五种具体解析器实现类）、XmlPathMapping（XML 节点路径到实体字段的映射接口）以及 XmlImportResult（导入结果封装对象，包含成功条数、失败条数及详细错误信息列表）。

### 流程图

```mermaid
flowchart TD
    A["用户选择XML文件上传"] --> B["XmlImportHandler 接收文件"]
    B --> C{"根据检查类型选择对应解析器"}
    C -->|CT| D["CtXmlParser 解析"]
    C -->|MRI| E["MriXmlParser 解析"]
    C -->|病理| F["PathologyXmlParser 解析"]
    C -->|肠镜| G["EnteroscopyXmlParser 解析"]
    C -->|检验| H["LabXmlParser 解析"]
    D --> I["使用 DOM 解析器加载 XML 文档"]
    E --> I
    F --> I
    G --> I
    H --> I
    I --> J["按 XmlPathMapping 提取字段值"]
    J --> K{"日期字段兼容解析"}
    K --> L["补全创建时间与修改时间"]
    L --> M{"患者编号是否已在系统内存在?"}
    M -->|否| N["记录失败，返回患者不存在"]
    M -->|是| O["调用对应Service写入检查记录表"]
    O --> P["返回 XmlImportResult"]
    P --> Q["前端展示导入结果"]
```

### 时序图

LabXmlParser 相比其他四种解析器多了一个 resolveItemId 私有方法：当 XML 中携带的检验项目代码或名称在系统字典表中无法精确匹配时，该方法先按项目代码匹配，匹配不到再按项目名称进行二次匹配，两次均失败则抛出异常并指明具体的记录序号与字段名称。

```mermaid
sequenceDiagram
    actor 用户 as 用户
    participant Handler as XmlImportHandler
    participant Parser as LabXmlParser
    participant Base as AbstractXmlParser
    participant Dict as LabItemDictMapper
    participant Service as LabResultService
    participant DB as lab_result表

    用户->>Handler: POST /import/lab/xml
    Handler->>Parser: parse(MultipartFile)
    Parser->>Base: 调用父类parse方法加载XML
    Base->>Base: DocumentBuilderFactory解析XML文档
    Base->>Parser: 回调parseRecords(Document)
    loop 遍历XML中每条检验记录
        Parser->>Base: getElementText 提取字段值
        Parser->>Dict: 按itemCode查询检验项目
        alt 编码匹配失败
            Parser->>Dict: 按itemName二次匹配
            alt 名称也匹配失败
                Parser->>Parser: 记录失败:第N条项目不存在
            end
        end
        Base->>Base: parseDateTime 兼容多种日期格式
        Parser->>Parser: 组装 LabResult 实体
    end
    Parser-->>Handler: XmlImportResult(成功/失败明细)
    Handler->>Service: saveBatch(成功记录列表)
    Service->>DB: 批量INSERT
    DB-->>Service: 写入完成
    Handler-->>用户: 返回导入结果
```

## 5.1.4 检验结果管理

检验结果管理实现检验指标的逐项录入与多项目批量登记。单条录入时系统自动比对检验项目字典中的参考范围，超出范围的数值前端高亮标记；批量登记时一次提交多条检验结果，后端使用 @Transactional 事务控制保证原子性——全部成功则提交，任意一条失败则回滚。设计上，LabResultController 对外暴露两个核心写入接口：/labResult/save（单条录入）和 /labResult/batchSave（批量登记），LabResultServiceImpl 在继承 ServiceImpl 的基础上额外注入了 LabItemDictMapper（用于参照范围比对）和 WarningEngineService（用于异步触发预警评估）。

### 流程图

```mermaid
flowchart TD
    A["检验技师录入/批量登记检验结果"] --> B["LabResultController接收"]
    B --> C{"录入模式判断"}
    C -->|单条录入| D["逐项填写检验项目与结果值"]
    C -->|批量登记| E["勾选多项检验指标统一提交"]
    D --> F["Controller校验必填字段"]
    E --> G["Controller开启事务"]
    F --> H["Service层写入单条记录"]
    G --> I["遍历检验项目数组"]
    I --> J["逐项查询参考范围并比对"]
    J --> K["标记超出参考范围的项目"]
    K --> L["调用Mapper逐条写入"]
    L --> M{"全部写入成功?"}
    M -->|是| N["提交事务"]
    M -->|否| O["回滚事务，返回失败"]
    H --> P["异步触发预警评估"]
    N --> P
    P --> Q["返回登记结果"]
```

### 时序图

```mermaid
sequenceDiagram
    actor 技师 as 检验技师
    participant Controller as LabResultController
    participant Service as LabResultServiceImpl
    participant DictMapper as LabItemDictMapper
    participant LabMapper as LabResultMapper
    participant DB as lab_result表
    participant Warning as WarningEngineService

    技师->>Controller: POST /labResult/batchSave
    Controller->>Service: batchSave(patientId, List<LabItem>)
    Service->>Service: 开启事务
    loop 遍历每项检验指标
        Service->>DictMapper: 查询项目编码与参考范围
        DictMapper-->>Service: 返回 itemId, unit, normalRange
        Service->>Service: 比对结果值与参考范围
        Service->>LabMapper: insert(labResult)
        LabMapper->>DB: INSERT INTO lab_result
        DB-->>LabMapper: 写入成功
    end
    alt 全部成功
        Service->>Service: 提交事务
        Service->>Warning: 异步调用预警评估引擎
        Warning-->>Service: 已接收(异步执行)
        Service-->>Controller: 批量登记成功
    else 任意一条失败
        Service->>Service: 回滚事务
        Service-->>Controller: 批量登记失败
    end
    Controller-->>技师: 返回操作结果
```

## 5.1.5 医嘱管理

医嘱管理模块在实现上相对独立，涉及 MedicalOrderController、MedicalOrderServiceImpl、MedicalOrderMapper 和 MedicalOrder 实体四个核心组件。Controller 对外暴露 /medicalOrder/save（新增）、/medicalOrder/update（修改）和 /medicalOrder/page（分页查询）三个接口。修改医嘱时系统不覆盖原始记录而是新增一行并将 hospitalizationTimes 递增作为版本标记，查询时取最新版本展示，保留了完整的医嘱变更历史。

### 流程图

```mermaid
flowchart TD
    A["医生填写医嘱内容"] --> B["MedicalOrderController接收"]
    B --> C{"操作类型判断"}
    C -->|新增| D["设置 hospitalizationTimes 为 1"]
    C -->|修改| E["查询原医嘱记录"]
    E --> F["hospitalizationTimes 递增作为新版本"]
    C -->|查询| G["按患者编号与时间范围分页查询"]
    D --> H["获取当前登录用户为开嘱医生"]
    F --> H
    H --> I["记录开嘱时间"]
    I --> J["调用 Mapper 写入 medical_order 表"]
    J --> K["AOP切面记录操作日志"]
    G --> L["返回医嘱分页列表"]
    K --> M["返回操作成功"]
```

### 时序图

```mermaid
sequenceDiagram
    actor 医生 as 医生
    participant Controller as MedicalOrderController
    participant Service as MedicalOrderServiceImpl
    participant Mapper as MedicalOrderMapper
    participant DB as medical_order表
    participant AOP as OperateLogAspect

    医生->>Controller: POST /medicalOrder/update
    Controller->>Service: updateOrder(MedicalOrder)
    Service->>Mapper: selectById(orderId)
    Mapper->>DB: SELECT 原记录
    DB-->>Mapper: 返回原记录
    Mapper-->>Service: 原 MedicalOrder 对象
    Service->>Service: 将 hospitalizationTimes 递增
    Service->>Service: 更新 startTime 与 orderContent
    Service->>Mapper: insert 新版本记录
    Mapper->>DB: INSERT INTO medical_order
    DB-->>Mapper: 插入成功
    Mapper-->>Service: 返回新记录
    Service-->>Controller: 返回新版本医嘱
    Controller->>AOP: @OperateLog 触发
    AOP->>AOP: 对比新旧版本数据快照
    AOP->>AOP: 异步写入操作日志
    Controller-->>医生: 修改成功
```

## 日常诊疗模块总类图

下面这张类图汇总了日常诊疗模块全部核心类及其依赖关系。十四行摘要说明如下：

- 患者管理线：PatientController → IPatientService/PatientServiceImpl → PatientMapper，PatientServiceImpl 额外持有四类检查 Mapper 和两个关联 Mapper 的引用，专为全景视图跨表聚合服务
- CT 检查线：CtExaminationController → ICtExaminationService/CtExaminationServiceImpl → CtExaminationMapper → CtExamination 实体
- 核磁检查线：MriExaminationController → IMriExaminationService/MriExaminationServiceImpl → MriExaminationMapper → MriExamination 实体
- 病理检查线：PathologyExaminationController → IPathologyExaminationService/PathologyExaminationServiceImpl → PathologyExaminationMapper → PathologyExamination 实体
- 肠镜检查线：EnteroscopyExaminationController → IEnteroscopyExaminationService/EnteroscopyExaminationServiceImpl → EnteroscopyExaminationMapper → EnteroscopyExamination 实体
- 检验管理线：LabResultController → ILabResultService/LabResultServiceImpl（额外注入 LabItemDictMapper 和 WarningEngineService） → LabResultMapper → LabResult 实体
- 医嘱管理线：MedicalOrderController → IMedicalOrderService/MedicalOrderServiceImpl → MedicalOrderMapper → MedicalOrder 实体
- 附件管理线：Attachment 实体被各检查 Controller 在报告上传时引用，AttachmentMapper 提供独立的附件元信息持久化
- XML 导入线：ImportController 聚合 XmlImportHandler，后者内部根据文件类型动态选择 CtXmlParser/MriXmlParser/EnteroscopyXmlParser/PathologyXmlParser/LabXmlParser 中的一种，每种解析器继承自 AbstractXmlParser
- AOP 横切线：OperateLogAspect 作为切面类以虚线关联所有标注了 @OperateLog 的 Controller 方法，在方法执行前后自动抓取数据快照并通过 IOperationLogService 异步写入操作日志表
- 实体层：所有实体类（Patient / CtExamination / MriExamination / PathologyExamination / EnteroscopyExamination / LabResult / MedicalOrder / Attachment / LabItemDict）均用 MyBatis-Plus 的 @TableName 注解映射对应数据库表
- Mapper 层：所有 Mapper 接口统一继承 BaseMapper，具备内置 CRUD 能力，仅复杂联合查询或统计聚合通过自定义 XML 映射文件实现
- Service 层：四类检查的 ServiceImpl 直接继承 ServiceImpl，无需任何额外代码；PatientServiceImpl 和 LabResultServiceImpl 因业务复杂度较高，在继承基础上额外注入关联依赖
- 类图图例：实线箭头「→」表示依赖注入（@Autowired），空心三角箭头「▷」表示接口实现，闭合三角箭头「▷」表示继承，虚线箭头「- - >」表示 AOP 切面拦截

```mermaid
classDiagram
    class PatientController {
        +save(Patient) Result
        +update(Patient) Result
        +delete(String) Result
        +restore(String) Result
        +overview(String) Result
    }
    class IPatientService {
        <<interface>>
        +addPatient(Patient) Patient
        +updatePatient(Patient) boolean
        +getPatientOverview(String) PatientOverviewVO
    }
    class PatientServiceImpl {
        -PatientMapper patientMapper
        -CtExaminationMapper ctMapper
        -MriExaminationMapper mriMapper
        -PathologyExaminationMapper pathMapper
        -EnteroscopyExaminationMapper enteroMapper
        -LabResultMapper labMapper
        -MedicalOrderMapper orderMapper
        +addPatient(Patient) Patient
        +getPatientOverview(String) PatientOverviewVO
    }
    class PatientMapper {
        +insert(Patient) int
        +selectById(String) Patient
        +selectList(Wrapper) List
    }
    class Patient {
        -String patientId
        -String patientName
        -String gender
        -Integer age
        -String phone
        -String idCard
        -Boolean isInvalid
    }
    class PatientOverviewVO {
        -List~CtExamination~ ctList
        -List~MriExamination~ mriList
        -List~PathologyExamination~ pathList
        -List~EnteroscopyExamination~ enteroList
        -List~LabResult~ labList
        -List~MedicalOrder~ orderList
    }

    class CtExaminationController {
        +save(CtExamination) Result
        +update(CtExamination) Result
        +delete(Long) Result
        +upload(MultipartFile,Long) Result
        +download(Long,HttpServletResponse) void
    }
    class ICtExaminationService {
        <<interface>>
    }
    class CtExaminationServiceImpl {
    }
    class CtExaminationMapper {
        +selectList(Wrapper) List
    }
    class CtExamination {
        -Long ctId
        -String patientId
        -String examinationNo
        -LocalDateTime examinationTime
        -String examinationPart
        -String examineDoctor
        -String examineDept
        -String reportUrl
        -String imageUrl
        -String reportConclusion
        -Integer userId
        -Boolean isInvalid
    }

    class MriExaminationController {
        +save(MriExamination) Result
        +update(MriExamination) Result
        +upload(MultipartFile,Long) Result
    }
    class IMriExaminationService {
        <<interface>>
    }
    class MriExaminationServiceImpl {
    }
    class MriExaminationMapper {
    }
    class MriExamination {
        -Long mriId
        -String patientId
        -String examinationNo
        -LocalDateTime examinationTime
        -String examinationPart
        -String examineDoctor
        -String examineDept
        -String reportUrl
        -String imageUrl
        -String reportConclusion
        -Integer userId
        -Boolean isInvalid
    }

    class PathologyExaminationController {
        +save(PathologyExamination) Result
        +upload(MultipartFile,Long) Result
    }
    class IPathologyExaminationService {
        <<interface>>
    }
    class PathologyExaminationServiceImpl {
    }
    class PathologyExaminationMapper {
    }
    class PathologyExamination {
        -Long pathologyId
        -String patientId
        -String pathologyNo
        -String specimenType
        -LocalDateTime samplingTime
        -LocalDateTime reportTime
        -String pathologyDoctor
        -String pathologyDept
        -String reportUrl
        -String slideImageUrl
        -String pathologyDiagnosis
        -Integer userId
        -Boolean isInvalid
    }

    class EnteroscopyExaminationController {
        +save(EnteroscopyExamination) Result
        +upload(MultipartFile,Long) Result
    }
    class IEnteroscopyExaminationService {
        <<interface>>
    }
    class EnteroscopyExaminationServiceImpl {
    }
    class EnteroscopyExaminationMapper {
    }
    class EnteroscopyExamination {
        -Long enteroscopyId
        -String patientId
        -String examinationNo
        -LocalDateTime examinationTime
        -String enteroscopyType
        -String examineDoctor
        -String examineDept
        -String reportUrl
        -String imageUrl
        -String reportConclusion
        -Integer userId
        -Boolean isInvalid
    }

    class LabResultController {
        +save(LabResult) Result
        +batchSave(List~LabResult~) Result
        +getTrend(String,int) Result
    }
    class ILabResultService {
        <<interface>>
        +batchSave(String,List) boolean
        +getTrendData(String,int) List~TrendVO~
    }
    class LabResultServiceImpl {
        -LabResultMapper labMapper
        -LabItemDictMapper dictMapper
        -WarningEngineService warningEngine
        +batchSave(String,List) boolean
        +getTrendData(String,int) List~TrendVO~
    }
    class LabResultMapper {
    }
    class LabResult {
        -Long resultId
        -String patientId
        -Integer itemId
        -BigDecimal resultValue
        -String resultUnit
        -LocalDateTime reportTime
        -String executeDept
        -String executeDoc
        -Boolean isInvalid
    }
    class LabItemDictMapper {
        +selectByCode(String) LabItemDict
        +selectList(Wrapper) List
    }
    class LabItemDict {
        -Integer itemId
        -String itemName
        -String itemCode
        -String defaultUnit
        -String normalRange
    }

    class MedicalOrderController {
        +save(MedicalOrder) Result
        +update(MedicalOrder) Result
        +page(QueryPageParam) Result
    }
    class IMedicalOrderService {
        <<interface>>
    }
    class MedicalOrderServiceImpl {
    }
    class MedicalOrderMapper {
    }
    class MedicalOrder {
        -Long orderId
        -Integer hospitalizationTimes
        -String patientId
        -String orderName
        -LocalDateTime startTime
        -LocalDateTime endTime
        -String orderStatus
        -String orderFrequency
        -String executeDept
        -String executeDoc
        -Boolean isInvalid
    }

    class Attachment {
        -Long attachmentId
        -String patientId
        -String attachmentName
        -String attachmentType
        -String storageUrl
        -Integer userId
        -Long relatedResultId
        -Boolean isInvalid
    }
    class AttachmentMapper {
    }

    class ImportController {
        +importCtXml(MultipartFile) Result
        +importLabXml(MultipartFile) Result
    }
    class XmlImportHandler {
        -LabItemDictMapper dictMapper
        -IPatientService patientService
        -ICtExaminationService ctService
        -IMriExaminationService mriService
        -IPathologyExaminationService pathService
        -IEnteroscopyExaminationService enteroService
        -ILabResultService labService
        +importXml(MultipartFile,String) XmlImportResult
    }
    class AbstractXmlParser {
        <<abstract>>
        +parse(MultipartFile) XmlImportResult
        #parseDateTime(String) LocalDateTime
        #getElementText(Element,String) String
        #parseRecords(Document) List
    }
    class CtXmlParser {
        +parseRecords(Document) List~CtExamination~
    }
    class MriXmlParser {
        +parseRecords(Document) List~MriExamination~
    }
    class EnteroscopyXmlParser {
        +parseRecords(Document) List~EnteroscopyExamination~
    }
    class PathologyXmlParser {
        +parseRecords(Document) List~PathologyExamination~
    }
    class LabXmlParser {
        -LabItemDictMapper dictMapper
        +parseRecords(Document) List~LabResult~
        -resolveItemId(String,String) Integer
    }
    class XmlPathMapping {
        <<interface>>
    }

    class OperateLogAspect {
        -IOperationLogService logService
        +around(ProceedingJoinPoint) Object
        -saveLogAsync(OperationLogContext) void
    }
    class OperateLog {
        -Long logId
        -Integer userId
        -String operationType
        -String operatedTable
        -String beforeContent
        -String afterContent
    }

    PatientController --> IPatientService
    IPatientService <|.. PatientServiceImpl
    PatientServiceImpl --> PatientMapper
    PatientServiceImpl --> CtExaminationMapper
    PatientServiceImpl --> MriExaminationMapper
    PatientServiceImpl --> PathologyExaminationMapper
    PatientServiceImpl --> EnteroscopyExaminationMapper
    PatientServiceImpl --> LabResultMapper
    PatientServiceImpl --> MedicalOrderMapper
    PatientMapper ..> Patient
    PatientServiceImpl ..> PatientOverviewVO

    CtExaminationController --> ICtExaminationService
    ICtExaminationService <|.. CtExaminationServiceImpl
    CtExaminationServiceImpl --> CtExaminationMapper
    CtExaminationMapper ..> CtExamination

    MriExaminationController --> IMriExaminationService
    IMriExaminationService <|.. MriExaminationServiceImpl
    MriExaminationServiceImpl --> MriExaminationMapper
    MriExaminationMapper ..> MriExamination

    PathologyExaminationController --> IPathologyExaminationService
    IPathologyExaminationService <|.. PathologyExaminationServiceImpl
    PathologyExaminationServiceImpl --> PathologyExaminationMapper
    PathologyExaminationMapper ..> PathologyExamination

    EnteroscopyExaminationController --> IEnteroscopyExaminationService
    IEnteroscopyExaminationService <|.. EnteroscopyExaminationServiceImpl
    EnteroscopyExaminationServiceImpl --> EnteroscopyExaminationMapper
    EnteroscopyExaminationMapper ..> EnteroscopyExamination

    LabResultController --> ILabResultService
    ILabResultService <|.. LabResultServiceImpl
    LabResultServiceImpl --> LabResultMapper
    LabResultServiceImpl --> LabItemDictMapper
    LabResultServiceImpl --> WarningEngineService
    LabResultMapper ..> LabResult
    LabItemDictMapper ..> LabItemDict

    MedicalOrderController --> IMedicalOrderService
    IMedicalOrderService <|.. MedicalOrderServiceImpl
    MedicalOrderServiceImpl --> MedicalOrderMapper
    MedicalOrderMapper ..> MedicalOrder

    ImportController --> XmlImportHandler
    XmlImportHandler --> IPatientService
    XmlImportHandler --> ICtExaminationService
    XmlImportHandler --> ILabResultService
    AbstractXmlParser <|-- CtXmlParser
    AbstractXmlParser <|-- MriXmlParser
    AbstractXmlParser <|-- EnteroscopyXmlParser
    AbstractXmlParser <|-- PathologyXmlParser
    AbstractXmlParser <|-- LabXmlParser
    AbstractXmlParser ..> XmlPathMapping
    LabXmlParser --> LabItemDictMapper
    XmlImportHandler --> AbstractXmlParser

    OperateLogAspect --> IOperationLogService
    OperateLogAspect ..> PatientController
    OperateLogAspect ..> CtExaminationController
    OperateLogAspect ..> MedicalOrderController
    OperateLogAspect ..> LabResultController
    IOperationLogService ..> OperateLog
```
